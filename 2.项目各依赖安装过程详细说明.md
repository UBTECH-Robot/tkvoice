# TKVoice install.sh 详细安装说明

本文档适合想详细了解各依赖的详细安装过程的用户

## 📋 目录
1. [安装前准备](#安装前准备)
2. [脚本整体流程](#脚本整体流程)
3. [分步详解](#分步详解)
4. [关键概念](#关键概念)
5. [常见问题](#常见问题)
6. [手动恢复指南](#手动恢复指南)

---

## 安装前准备

### 系统环境要求

| 项目 | 需求 | 说明 |
|------|------|------|
| 密钥认证 | SSH 公钥 | 无密码 SSH 连接 |
| 磁盘空间 | 13GB左右 | 存储模型和依赖 |

### 文件准备

```
工作目录/
└── tkvoice_release_0.3.12_0316_095057.tar  (安装包)
```

执行解压：
```bash
tar -xvf tkvoice_release_0.3.12_0316_095057.tar tkvoice_release_0.3.12_0316_095057/install.sh
```
得到如下目录结构：
```
工作目录/
├── tkvoice_release_0.3.12_0316_095057.tar  (安装包)
└── tkvoice_release_0.3.12_0316_095057/
    └── install.sh
```


## 执行安装
进入目录，执行安装脚本：
```bash
cd tkvoice_release_0.3.12_0316_095057
chmod +x install.sh
./install.sh
```

> 执行 install.sh 的时候会多次要求输入密码，注意看清楚是需要输入 x86 上 ubuntu 用户的密码还是 Orin 板上 nvidia 用户的密码


该安装脚本会做以下操作：
1. 将 docker_funasr 需要的文件传到 x86上，并在 x86 板上安装 docker, 导入镜像，启动容器运行 Funasr 服务
2. 在 orin 板上安装 ollama 服务，导入 qwen2.5_1.5b.tar.gz 大语言模型
3. 安装必要的 Python 包，包括 piper-tts, onnxruntime-gpu, httpx, websockets

如果install.sh脚本顺利执行成功，则安装tkvoice项目已完成安装。


## 测试运行
先登录到 41.2 的 Orin 板，进入目录：
```bash
cd tkvoice_release_0.3.12_0316_095057

然后可用如下命令进行管理:
# 启动服务
./tkvoice.sh start

# 停止服务
./tkvoice.sh stop

# 重启服务
./tkvoice.sh restart

# 查看状态
./tkvoice.sh status

# 查看日志"
tail -f /home/nvidia/tkvoice_release_0.3.12_0316_095057/tkvoice.log

```

> 如果到这一步都顺利完成，说明整体流程已通，tkvoice后续的文档可有选择性的浏览，只浏览自己需要的。


## 卸载

由于本项目占用大量磁盘空间，在紧急需要磁盘空间的时候，可卸载本项目，后续需要的时候再安装回来即可。

执行 uninstall.sh 的时候会多次要求输入密码，注意看清楚是需要输入 x86 上 ubuntu 用户的密码还是 Orin 板上 nvidia 用户的密码。


> 卸载脚本会卸载 Orin 板上的 ollama 和 x86 上的 Funasr 容器和镜像以及 docker 服务，请谨慎使用。

> 执行uninstall.sh 后 ~/tkvoice_release_0.3.12_0316_095057 目录会删除掉，也就是卸载脚本自身也会删除掉，但是 ~/tkvoice_release_0.3.12_0316_095057.tar 文件不会删除。

```bash
cd ~/tkvoice_release_0.3.12_0316_095057
chmod +x uninstall.sh
./uninstall.sh
```

---

## 脚本整体流程

### 流程图

```
开始
  ↓
[步骤1] 配置远程服务器信息
  ↓
[步骤2] 检查本地目录
  ↓
[步骤3] 传输 docker_funasr 到 x86
  ↓
[步骤4] 在 x86 上执行 ASR 安装
  ↓
[步骤5] 删除本地临时文件
  ↓
[步骤6] 安装 Python 依赖包
  ↓
[步骤7] 安装 Ollama 和模型
  ↓
[步骤8] 解压项目并编译 ROS2
  ↓
[步骤9] 输出完成信息
  ↓
完成 ✅
```

### 涉及的机器

| 步骤 | 执行位置 | 主要操作 |
|------|---------|--------|
| 1-5 | Orin 板 + x86 | 部署 Funasr ASR 服务 |
| 6 | Orin 板 | 安装 Python 第三方库 |
| 7 | Orin 板 | 安装 Ollama LLM |
| 8 | Orin 板 | 编译 ROS 2 项目 |

---

## 分步详解

### 步骤 1：配置远程服务器信息

```bash
REMOTE_USER="ubuntu"      # x86 服务器登录用户名
REMOTE_IP="192.168.41.1"  # x86 服务器 IP
REMOTE_DIR="/home/ubuntu" # x86 上的工作目录

RELEASE_DIR="tkvoice_release_0.3.12_0316_095057"  # 解压目录名
```

**作用**：定义连接参数，后续所有远程操作都基于这些变量。

### 步骤 2：验证安装包完整性

```bash
# 检查 tar 包是否存在
if [ ! -f "${RELEASE_DIR}.tar" ]; then
    echo "[ERROR] 找不到 ${RELEASE_DIR}.tar"
    exit 1
fi

# 检查 tar 包内是否有必需的目录
if ! tar -tf "${RELEASE_DIR}.tar" | grep -q "${RELEASE_DIR}/res/docker_funasr/"; then
    echo "[ERROR] tar 包中未找到 ${RELEASE_DIR}/res/docker_funasr/"
    exit 1
fi
```

**作用**：防止后续出现错误。

**原理**：
- `[ ! -f file ]` - 检查文件是否不存在
- `tar -tf` - 列出 tar 包内容（不解压）
- `grep -q` - 静默搜索（只返回 0/1）

**失败排查**：
```bash
# 检查 tar 包是否损坏
tar -tzf "${RELEASE_DIR}.tar" > /dev/null

# 列出 tar 包中的目录
tar -tf "${RELEASE_DIR}.tar" | head -20
```

### 步骤 3：解压 ASR 相关文件到本地

```bash
cd "${PARENT_DIR}"
tar -xvf "${RELEASE_DIR}.tar" \
    "${RELEASE_DIR}/uninstall.sh" \
    "${RELEASE_DIR}/res/docker_funasr/"
```

**作用**：只解压 ASR 相关的文件（节省时间和空间），不解压整个包。

**参数说明**：
- `-x` : 解压
- `-v` : 显示详细信息
- `-f` : 指定文件
- 最后的参数是要解压的路径（可以指定多个）

**结果**：
```
当前目录/
└── tkvoice_release_0.3.12_0316_095057/
    ├── uninstall.sh
    ├── res/
    │   └── docker_funasr/
    │       ├── install_asr.sh
    │       ├── startup.sh
    │       ├── uninstall_asr.sh
    │       └── ... (其他 Docker 相关文件)
    └── ... (其他暂未解压)
```

### 步骤 4：传输 docker_funasr 到 x86 服务器

```bash
rsync -av --progress --delete -e "ssh -o StrictHostKeyChecking=no" \
    res/docker_funasr \
    ${REMOTE_USER}@${REMOTE_IP}:${REMOTE_DIR}/
```

**作用**：通过 SSH 安全传输 ASR Docker 文件到 x86。

**rsync 参数详解**：

| 参数 | 作用 | 说明 |
|------|------|------|
| `-a` | archive 模式 | 保留权限、时间戳、符号链接 |
| `-v` | verbose | 显示详细信息 |
| `--progress` | 显示进度 | 每个文件的传输百分比 |
| `--delete` | 删除多余文件 | 远程目录与本地完全同步 |
| `-e ssh` | 使用 SSH | 而不是默认的 rsh |
| `StrictHostKeyChecking=no` | 跳过密钥确认 | 无需手动输入 yes |

**传输过程示例**：

```
sending incremental file list
res/docker_funasr/
res/docker_funasr/install_asr.sh
           1.2K 100%    1.15MB/s    0:00:00 (xfer#1, to-check=5/7)
res/docker_funasr/startup.sh
           2.5K 100%    2.43MB/s    0:00:00 (xfer#2, to-check=4/7)
...

sent 2,500 bytes  received 35 bytes  5,070 bytes/sec
total size is 50,000  speedup is 19.92
```

**如果传输失败**：
```bash
# 检查 SSH 连接
ssh -i ~/.ssh/id_rsa ubuntu@192.168.41.1 "echo 'SSH 连接成功'"

# 检查目标目录权限
ssh ubuntu@192.168.41.1 "ls -la /home/ubuntu/"

# 手动传输（如果 rsync 不可用）
scp -r res/docker_funasr ubuntu@192.168.41.1:/home/ubuntu/
```

### 步骤 5：在 x86 上执行 ASR 安装脚本

```bash
ssh -t ${REMOTE_USER}@${REMOTE_IP} \
    "cd ${REMOTE_DIR}/docker_funasr && bash install_asr.sh"
```

**作用**：在 x86 服务器上远程执行 ASR Docker 安装。

**参数说明**：
- `-t` : 分配伪终端（交互式 SSH）
- 命令被引号括起来，作为远程服务器执行的脚本

**实际执行的内容**：

x86 上的 `install_asr.sh` 会做以下事情：
1. 安装 Docker（如果未安装）
2. 加载或拉取 Funasr Docker 镜像
3. 启动 Funasr 容器，监听 192.168.41.1:10097

**监控远程执行**：

如果需要看到远程执行的详细输出，可以修改为：

```bash
ssh ${REMOTE_USER}@${REMOTE_IP} \
    "cd ${REMOTE_DIR}/docker_funasr && bash -x install_asr.sh"
# -x 参数会打印每条执行的命令
```

### 步骤 6：删除本地临时 ASR 文件

```bash
cd "${BASE_DIR}/res"
rm -rf docker_funasr
```

**作用**：释放本地磁盘空间，ASR 文件已同步到 x86，无需保留副本。

### 步骤 7：安装 Python 依赖包

#### 7.1 解压依赖包

```bash
tar -xvf "${RELEASE_DIR}.tar" \
    "${RELEASE_DIR}/res/onnxruntime-1.23.2-cp310-cp310-manylinux_2_27_aarch64.manylinux_2_28_aarch64.whl" \
    "${RELEASE_DIR}/res/onnxruntime_gpu-1.20.1-cp310-cp310-linux_aarch64.whl" \
    "${RELEASE_DIR}/res/piper_tts-1.4.1-cp39-abi3-manylinux_2_17_aarch64.manylinux2014_aarch64.manylinux_2_28_aarch64.whl"
```

**作用**：只解压需要的 Python 包（wheel 格式）。

**包的说明**：

| 包名 | 版本 | 用途 | 说明 |
|------|------|------|------|
| onnxruntime | 1.23.2 | CPU 推理 | Piper TTS 和其他 ONNX 模型 |
| onnxruntime_gpu | 1.20.1 | GPU 推理 | 使用 Orin 的 GPU 加速 |
| piper_tts | 1.3.0 | TTS 合成 | 文本转语音库 |

**文件名格式说明**：

```
piper_tts-1.4.1-cp39-abi3-manylinux_2_17_aarch64.manylinux2014_aarch64.manylinux_2_28_aarch64.whl
├─ 1.3.0          : 包版本
├─ cp39           : CPython 3.9+
├─ abi3           : 稳定 ABI（向后兼容）
├─ aarch64        : ARM 64 位架构
└─ manylinux_2_x  : Linux 发行版兼容性
```

#### 7.2 卸载旧版本

```bash
python3 -m pip uninstall onnxruntime piper-tts onnxruntime-gpu -y || \
sudo python3 -m pip uninstall onnxruntime piper-tts onnxruntime-gpu -y || \
true
```

**作用**：清除旧版本，防止版本冲突。

**参数说明**：
- `-y` : 自动答 yes，无需交互
- `|| true` : 即使命令失败也继续（避免整个脚本退出）
- `sudo` : 备选方案（如果用户权限不足）

#### 7.3 安装 ONNX Runtime CPU 和 TTS

```bash
python3 -m pip install --no-cache-dir \
    onnxruntime-1.23.2-cp310-cp310-manylinux_2_27_aarch64.manylinux_2_28_aarch64.whl \
    piper_tts-1.4.1-cp39-abi3-manylinux_2_17_aarch64.manylinux2014_aarch64.manylinux_2_28_aarch64.whl \
    httpx==0.28.1 \
    websockets==15.0.1
```

**作用**：安装 TTS 和网络库。

**参数说明**：
- `--no-cache-dir` : 不保存缓存，节省磁盘空间
- `.whl` 文件 : 本地轮包（预编译）
- `httpx` : 异步 HTTP 客户端（用于 Ollama）
- `websockets` : WebSocket 库（用于 Funasr 通信）

**版本选择原因**：
- `httpx==0.28.1` : 与 Ollama 客户端兼容
- `websockets==15.0.1` : 与 Funasr 通信兼容

#### 7.4 卸载 CPU 版 ONNX Runtime

```bash
python3 -m pip uninstall onnxruntime -y || \
sudo python3 -m pip uninstall onnxruntime -y || \
true
```

**作用**：移除 CPU 版本，为 GPU 版本让位。

#### 7.5 安装 GPU 版 ONNX Runtime

```bash
python3 -m pip install --no-cache-dir \
    onnxruntime_gpu-1.20.1-cp310-cp310-linux_aarch64.whl
```

**作用**：安装 GPU 加速版本，使 TTS 推理更快。

**性能对比**：
- CPU 版本：生成 1 秒语音需要 ~0.5 秒
- GPU 版本：生成 1 秒语音需要 ~0.1 秒（5 倍加速）

**为什么分开安装**：

CPU 版和 GPU 版的 onnxruntime 冲突，需要先安装 CPU 版（piper_tts 依赖），再替换为 GPU 版。

#### 7.6 清理 wheel 文件

```bash
rm -f *.whl
```

**作用**：删除已安装的 wheel 包，释放磁盘空间（可节省约 500MB）。

### 步骤 8：安装 Ollama

#### 8.1 解压 Ollama 脚本

```bash
tar -xvf "${RELEASE_DIR}.tar" \
    "${RELEASE_DIR}/res/ollama/uninstall_ollama.sh" \
    "${RELEASE_DIR}/res/ollama/install_ollama.sh" \
    "${RELEASE_DIR}/res/ollama/import_ollama_model.sh"
```

**作用**：解压 Ollama 安装和卸载脚本。

#### 8.2 执行 Ollama 安装

```bash
cd "${BASE_DIR}/res/ollama"
./install_ollama.sh "${PARENT_DIR}" "${RELEASE_DIR}"
```

**作用**：运行 Ollama 安装脚本。

**参数说明**：
- `${PARENT_DIR}` : 工作目录路径
- `${RELEASE_DIR}` : 解压目录名

**install_ollama.sh 会做什么**：
1. 检测 Ollama 是否已安装
2. 如果未安装，下载并安装 Ollama
3. 启动 Ollama 服务
4. 导入 qwen2.5:1.5b 模型

#### 8.3 清理 Ollama 临时文件

```bash
sudo find "${BASE_DIR}/res/ollama" \
    -mindepth 1 \
    ! -name 'uninstall_ollama.sh' \
    -exec rm -rf {} +
```

**作用**：删除 Ollama 临时文件，但保留卸载脚本。

**参数说明**：
- `-mindepth 1` : 不删除目录本身，只删除内容
- `! -name` : 排除（不删除）uninstall 脚本
- `-exec rm -rf {} +` : 对每个匹配的项执行删除

**结果**：保留卸载脚本以便后续维护。

### 步骤 9：解压主项目文件

```bash
tar -xvf "${RELEASE_DIR}.tar" \
    "${RELEASE_DIR}/tkvoice.sh" \
    "${RELEASE_DIR}/version.txt" \
    "${RELEASE_DIR}/res/piper_voices/" \
    "${RELEASE_DIR}/src/"
```

**作用**：解压项目源代码、启动脚本和 TTS 语音模型。

**关键文件**：

| 文件/目录 | 用途 | 说明 |
|----------|------|------|
| `tkvoice.sh` | 启动脚本 | 用于管理服务（启动/停止/重启） |
| `version.txt` | 版本信息 | 版本追踪 |
| `res/piper_voices/` | TTS 语音模型 | 中文语音（huayan-medium） |
| `src/` | 源代码 | audio_message 和 audio_service 包 |

### 步骤 10：编译 ROS 2 包

```bash
cd "${BASE_DIR}"
echo "[INFO] 开始编译 ROS2 包 audio_message 与 audio_service..."
colcon build --packages-select audio_message audio_service
echo "[OK] ROS2 包编译完成"
```

**作用**：使用 colcon 编译 ROS 2 包。

**编译过程**：

```
Starting >>> audio_message
Finished <<< audio_message [0.5s]
Starting >>> audio_service
[100%] Linking CXX executable ...
[100%] Built target ...
Finished <<< audio_service [5.2s]

Summary: 2 packages finished
```

**编译结果**：

```
${BASE_DIR}/
├── build/      # 中间构建文件
├── install/    # 安装的包（ROS 环境使用）
├── log/        # 编译日志
└── src/        # 源代码
```

**常见编译错误**：

| 错误 | 原因 | 解决 |
|------|------|------|
| `ament_cmake not found` | ROS 2 环境未初始化 | `source /opt/ros/humble/setup.bash` |
| `Permission denied` | 用户权限不足 | `sudo chown -R $USER:$USER build install` |
| `Package not found` | 依赖未安装 | 检查 package.xml 依赖 |

### 步骤 11：完成提示

```bash
echo "✅ tkvoice服务已成功安装，可用如下命令进行管理:"
echo ""
echo "# 启动服务"
echo "./tkvoice.sh start"
```

**作用**：提示用户安装完成和后续使用方法。

---

## 关键概念

### 什么是 .whl (Wheel) 文件？

Wheel 是 Python 的预编译包格式，相当于"已编译的安装程序"。

**vs 源码包 (tar.gz)**：
- 源码包 (tar.gz) : 包含源代码，安装时现场编译（慢，需要编译器）
- Wheel 包 (.whl) : 已编译，直接安装（快，无需编译）

**为什么使用 wheel**：
- Arm64 的编译环境复杂
- GPU 相关的编译容易出错
- 预编译可保证兼容性

### rsync vs scp

| 工具 | 特点 | 适用场景 |
|------|------|--------|
| rsync | 增量同步，支持 --delete | 大文件/目录，需要完全同步 |
| scp | 简单直接 | 小文件单次传输 |
| sftp | 交互式 | 需要管理文件 |

---

## 常见问题

### Q1: 安装中途断网了怎么办？

**A:** 重新运行 install.sh，脚本设计为可重复运行：

```bash
# 删除解压的文件
rm -rf tkvoice_release_0.3.12_0316_095057

cd ~
# 重新解压
tar -xvf tkvoice_release_0.3.12_0316_095057.tar tkvoice_release_0.3.12_0316_095057/install.sh
cd tkvoice_release_0.3.12_0316_095057
# 重新运行
bash install.sh
```

**原因**：每个步骤开始前都有检查，失败自动 exit。

### Q2: 权限不足的错误

**A:** 脚本已处理，但如果还有问题：

```bash
# 用 sudo 运行整个脚本
sudo bash install.sh

# 或者修改权限
sudo chown -R $USER:$USER ~
```

### Q3: SSH 连接超时

**A:** 检查网络和防火墙：

```bash
# 测试连接
ping 192.168.41.1

# 测试 SSH
ssh -v ubuntu@192.168.41.1

# 检查防火墙
sudo ufw status
sudo ufw allow 22/tcp
```

### Q4: Python 包安装冲突

**A:** 手动清理并重装：

```bash
# 卸载所有相关包
pip uninstall -y onnxruntime onnxruntime-gpu piper-tts httpx websockets

# 清理缓存
pip cache purge

# 重新运行脚本
bash install.sh
```

### Q5: colcon 编译失败

**A:** 检查 ROS 2 环境：

```bash
# 初始化 ROS 2
source /opt/ros/humble/setup.bash

# 清理旧构建
rm -rf build install log

# 重新编译
colcon build --packages-select audio_message audio_service --symlink-install
```

### Q6: 磁盘空间不足

**A:** 查看磁盘使用和清理：

```bash
# 查看磁盘
df -h

# 清理 pip 缓存
pip cache purge

# 删除旧的编译文件
rm -rf build log install

# 重新运行安装
bash install.sh
```

---

## 手动恢复指南

### 场景 1: 完全重新安装

```bash
# 停止所有服务
./tkvoice.sh stop  # 如果存在

# 完全删除本地安装
bash uninstall.sh  # 注意也会删除41.1的x86上的docker容器和镜像和docker服务，谨慎使用

# 如果只想删除Orin上的ollama及其模型，可用这个脚本
bash /home/nvidia/tkvoice_release_0.3.12_0316_095057/res/ollama/uninstall_ollama.sh

cd ~
# 重新解压
tar -xvf tkvoice_release_0.3.12_0316_095057.tar tkvoice_release_0.3.12_0316_095057/install.sh
cd tkvoice_release_0.3.12_0316_095057

# 重新安装
bash install.sh
```

### 场景 2: 仅重新安装 Ollama

```bash
# 卸载
bash /path/to/uninstall_ollama.sh

# 解压并重装（需要 tar 包）
tar -xf tkvoice_release_0.3.12_0316_095057.tar \
    tkvoice_release_0.3.12_0316_095057/res/ollama/

bash tkvoice_release_0.3.12_0316_095057/res/ollama/install_ollama.sh
```

### 场景 3: 仅重新编译 ROS 2

```bash
cd tkvoice_release_0.3.12_0316_095057

# 清理旧构建
rm -rf build install log

# 初始化环境
source /opt/ros/humble/setup.bash

# 重新编译
colcon build --packages-select audio_message audio_service

# 初始化新环境
source install/setup.bash
```

### 场景 4: 检查所有服务状态

```bash
# Orin 本地
./tkvoice.sh status
ps aux | grep ollama
ps aux | grep audio

# x86 远程
ssh ubuntu@192.168.41.1 'docker ps'
ssh ubuntu@192.168.41.1 'docker logs asr'
```

---

## 总结

| 步骤 | 目标 | 机器 | 关键命令 |
|------|------|------|--------|
| 1-5 | 部署 ASR | Orin + x86 | rsync, ssh, docker |
| 6 | 安装依赖 | Orin | pip install .whl |
| 7 | 安装 LLM | Orin | ollama install |
| 8 | 编译项目 | Orin | colcon build |
| 9 | 验证完成 | Orin | tkvoice.sh status |

**安装耗时**：
- 传输tar包：用网线直连则2-3分钟，用同个wifi下的局域网则取决于网速，tar包大小是6G左右，以平均10M/秒的速度可能也要10多分钟
- 解压和安装：时间有点长，3-5分钟，这个时间是无法省略的
- 首次下载模型：仅ollama模型需要尝试拉取模型的元数据，不需要拉取模型本身，网络正常正常的话时间基本可以忽略
- 编译 ROS2：时间不长
